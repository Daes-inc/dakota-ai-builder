import express from "express";
import cors from "cors";

const app = express();
const PORT = process.env.PORT || 3000;

app.use(cors());
app.use(express.json());

function makeProject(promptText) {
  const prompt = (promptText || "starter app").trim();
  const safePrompt = prompt.replace(/</g, "&lt;").replace(/>/g, "&gt;");
  const projectName = prompt.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, "") || "starter-app";

  const indexHtml = `<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>${safePrompt}</title>
  <link rel="stylesheet" href="style.css" />
</head>
<body>
  <main class="app-shell">
    <section class="card">
      <span class="badge">Generated App</span>
      <h1>${safePrompt}</h1>
      <p class="subtitle">
        This starter project was generated by Dakota AI Builder.
      </p>

      <div class="actions">
        <button id="primaryBtn">Get Started</button>
        <button id="secondaryBtn" class="secondary">Learn More</button>
      </div>

      <div class="status-box">
        <h2>Status</h2>
        <p id="statusText">Ready to run.</p>
      </div>
    </section>
  </main>

  <script src="script.js"></script>
</body>
</html>`;

  const styleCss = `:root {
  --bg: #0f172a;
  --panel: #1e293b;
  --panel-2: #0b1220;
  --text: #f8fafc;
  --muted: #cbd5e1;
  --primary: #22c55e;
  --secondary: #334155;
  --shadow: 0 16px 40px rgba(0, 0, 0, 0.28);
  --radius: 18px;
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  min-height: 100vh;
  font-family: Arial, sans-serif;
  background:
    radial-gradient(circle at top, #1d4ed8 0%, #0f172a 45%),
    #0f172a;
  color: var(--text);
}

.app-shell {
  min-height: 100vh;
  display: grid;
  place-items: center;
  padding: 24px;
}

.card {
  width: min(720px, 100%);
  background: rgba(30, 41, 59, 0.95);
  border-radius: var(--radius);
  box-shadow: var(--shadow);
  padding: 32px;
  text-align: center;
  border: 1px solid rgba(255,255,255,0.08);
}

.badge {
  display: inline-block;
  margin-bottom: 12px;
  padding: 6px 12px;
  border-radius: 999px;
  background: rgba(34, 197, 94, 0.15);
  color: #86efac;
  font-size: 13px;
  font-weight: 700;
  letter-spacing: 0.04em;
  text-transform: uppercase;
}

h1 {
  margin: 0 0 12px;
  font-size: clamp(2rem, 6vw, 4rem);
  line-height: 1.05;
}

.subtitle {
  margin: 0 auto 24px;
  max-width: 520px;
  color: var(--muted);
  font-size: 18px;
  line-height: 1.5;
}

.actions {
  display: flex;
  gap: 12px;
  justify-content: center;
  flex-wrap: wrap;
  margin-bottom: 24px;
}

button {
  border: none;
  border-radius: 10px;
  padding: 14px 22px;
  font-size: 16px;
  font-weight: 700;
  cursor: pointer;
  transition: transform 0.15s ease, opacity 0.15s ease;
}

button:hover {
  transform: translateY(-1px);
  opacity: 0.96;
}

#primaryBtn {
  background: var(--primary);
  color: #052e16;
}

.secondary {
  background: var(--secondary);
  color: var(--text);
}

.status-box {
  background: var(--panel-2);
  border-radius: 14px;
  padding: 18px;
  text-align: left;
}

.status-box h2 {
  margin: 0 0 8px;
  font-size: 18px;
}

.status-box p {
  margin: 0;
  color: var(--muted);
  line-height: 1.5;
}`;

  const scriptJs = `const statusText = document.getElementById("statusText");
const primaryBtn = document.getElementById("primaryBtn");
const secondaryBtn = document.getElementById("secondaryBtn");

primaryBtn.addEventListener("click", () => {
  statusText.textContent = "Primary action triggered. Your generated app is alive.";
});

secondaryBtn.addEventListener("click", () => {
  statusText.textContent = "Secondary action triggered. Customize this starter project however you want.";
});

console.log("${safePrompt} app loaded successfully.");`;

  const readmeMd = `# ${prompt}

Generated by Dakota AI Builder.

## Files

- \`index.html\` — main HTML structure
- \`style.css\` — layout and styling
- \`script.js\` — interactivity
- \`README.md\` — project notes

## How to use

1. Save all files in the same folder.
2. Open \`index.html\` in a browser.

## Suggested next upgrades

- Add pricing or subscription flow
- Add lead capture form
- Connect analytics
- Turn into React / Next.js app
- Prepare for deployment on Netlify or Vercel
`;

  return {
    status: "success",
    prompt,
    projectName,
    files: {
      "index.html": indexHtml,
      "style.css": styleCss,
      "script.js": scriptJs,
      "README.md": readmeMd
    }
  };
}

app.get("/", (req, res) => {
  res.send(`<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Dakota AI Builder</title>
  <style>
    body {
      margin: 0;
      min-height: 100vh;
      font-family: Arial, sans-serif;
      background: #0f172a;
      color: white;
      display: grid;
      place-items: center;
      padding: 24px;
      box-sizing: border-box;
    }
    .card {
      width: min(860px, 100%);
      background: #1e293b;
      border-radius: 18px;
      padding: 28px;
      box-shadow: 0 16px 40px rgba(0,0,0,0.28);
    }
    h1 {
      margin-top: 0;
      text-align: center;
      font-size: 42px;
    }
    p {
      text-align: center;
      color: #cbd5e1;
    }
    input {
      width: 100%;
      padding: 14px;
      border: none;
      border-radius: 10px;
      font-size: 16px;
      margin: 18px 0 14px;
      box-sizing: border-box;
    }
    button {
      display: block;
      margin: 0 auto 18px;
      background: #22c55e;
      color: #052e16;
      border: none;
      padding: 14px 24px;
      border-radius: 10px;
      font-size: 18px;
      font-weight: 700;
      cursor: pointer;
    }
    pre {
      background: #0b1220;
      color: #e2e8f0;
      padding: 16px;
      border-radius: 12px;
      white-space: pre-wrap;
      word-break: break-word;
      overflow-x: auto;
      min-height: 180px;
    }
  </style>
</head>
<body>
  <div class="card">
    <h1>Dakota AI Builder</h1>
    <p>Turn a prompt into a starter multi-file app project.</p>

    <input id="prompt" value="affiliate comparison site" />
    <button onclick="buildApp()">Build App</button>

    <pre id="result">Ready.</pre>
  </div>

  <script>
    async function buildApp() {
      const prompt = document.getElementById("prompt").value;

      const res = await fetch("/create-app", {
        method: "POST",
        headers: {
          "Content-Type": "application/json"
        },
        body: JSON.stringify({ prompt })
      });

      const data = await res.json();
      document.getElementById("result").textContent = JSON.stringify(data, null, 2);
    }
  </script>
</body>
</html>`);
});

app.get("/health", (req, res) => {
  res.json({
    status: "online",
    system: "Dakota AI Builder",
    version: "1.2"
  });
});

app.post("/create-app", (req, res) => {
  const result = makeProject(req.body?.prompt);
  res.json(result);
});

app.listen(PORT, () => {
  console.log("Dakota AI Builder running on port " + PORT);
});
