import express from "express";
import cors from "cors";

const app = express();
const PORT = process.env.PORT || 3000;

app.use(cors());
app.use(express.json());

app.get("/health", (req, res) => {
  res.json({
    status: "online",
    system: "Dakota AI Builder",
    version: "1.6"
  });
});

app.get("/", (req, res) => {
  res.send(`
<!DOCTYPE html>
<html>
<head>
<title>Dakota AI Builder</title>
<style>
body{
font-family:Arial;
background:#0f172a;
color:white;
text-align:center;
padding:40px;
margin:0;
}
input{
padding:12px;
width:320px;
border-radius:8px;
border:none;
margin:10px;
font-size:16px;
}
button{
padding:12px 20px;
border-radius:8px;
border:none;
background:#22c55e;
color:white;
font-size:16px;
cursor:pointer;
}
pre{
text-align:left;
background:#020617;
padding:20px;
margin-top:20px;
border-radius:10px;
max-width:700px;
margin-left:auto;
margin-right:auto;
overflow:auto;
}
</style>
</head>

<body>

<h1>Dakota AI Builder</h1>

<input id="prompt" value="fitness affiliate site"/>

<br>

<button onclick="deploy()">Build App</button>

<pre id="result">Ready</pre>

<script>
async function deploy(){

const prompt=document.getElementById("prompt").value;

const res=await fetch("/create-app",{
method:"POST",
headers:{"Content-Type":"application/json"},
body:JSON.stringify({prompt})
});

const data=await res.json();

document.getElementById("result").textContent=
JSON.stringify(data,null,2);

}
</script>

</body>
</html>
`);
});

app.post("/create-app", (req, res) => {

const prompt=req.body?.prompt || "starter website";

const projectName=prompt
.toLowerCase()
.replace(/[^a-z0-9]+/g,"-")
.replace(/^-|-$/g,"");

const indexHtml=`
<!DOCTYPE html>
<html>
<head>
<title>${prompt}</title>
</head>

<body style="font-family:Arial;background:#0f172a;color:white;text-align:center;padding:40px">

<h1>${prompt}</h1>
<p>Generated by Dakota AI Builder</p>

<button onclick="alert('App running')">
Get Started
</button>

</body>
</html>
`;

res.json({
status:"success",
prompt,
projectName,
files:{
"index.html":indexHtml
}
});

});

app.listen(PORT, () => {
console.log("Dakota AI Builder running on port",PORT);
});
